import React from "react";
import styled from "styled-components";

const StyledEvent = styled.section``;

const Event: React.FC = () => {
  return <StyledEvent>Event</StyledEvent>;
};

export default Event;
